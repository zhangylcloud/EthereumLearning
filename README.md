Install node with nvm can solve the EACCES problem:
nvm install:
https://github.com/nvm-sh/nvm#installation-and-update

Install node with nvm:
nvm install --lts
nvm use --lts

Install ganache-cli:
npm install -g ganache-cli
https://github.com/trufflesuite/ganache-cli
